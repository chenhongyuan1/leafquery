<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.leafquery.mapper.UserMapper">

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password, phone_number, email, avatar_url, role)
        VALUES (#{username}, #{password}, #{phoneNumber}, #{email}, #{avatarUrl}, #{role})
    </insert>

    <select id="selectByUsername" resultType="com.example.leafquery.entity.User">
        SELECT * FROM user WHERE username = #{username} OR phone_number = #{username} OR email = #{username}
    </select>

    <select id="selectByUserId" resultType="com.example.leafquery.entity.User">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <select id="selectAll" resultType="com.example.leafquery.entity.User">
        SELECT * FROM user
    </select>

    <update id="updateUser">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="phoneNumber != null">phone_number = #{phoneNumber},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="role != null">role = #{role},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteByUserId">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>


</mapper>
