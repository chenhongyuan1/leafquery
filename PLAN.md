# 新建竞赛版 Markdown 文档方案

## Summary
- 新建文件，默认路径为 `D:\LeafQuery\leafquery\病虫害趋势预测竞赛版方案.md`，不覆盖现有商用级落地计划。
- 文档定位为“开发实施版的完整竞赛方案”，面向后续直接落地开发，同时保留足够的评审叙事。
- 文档核心结论固定为：竞赛版不走重型 MLOps，不以 LSTM 为主线，采用“农业规则先验 + 气象时序特征 + GBDT 风险预测 + 7 天趋势展示”的方案。

## 文档结构与内容
- 标题与摘要：
  - 标题定为“病虫害趋势预测竞赛版实施方案”。
  - 开头用 1 段说明为何从原商用方案收缩为竞赛版：周期短、数据未沉淀、需优先保证可行性与展示效果。
- 1. 项目目标与范围：
  - 明确预测对象为“作物-地区-日风险”，输出当日风险和未来 7 天趋势。
  - 明确首版只覆盖 2-3 种作物，不做全国全病种、单田块精细预测、影子部署、模型热切换。
- 2. 作物与病虫害范围：
  - 默认写死为水稻、玉米、小麦。
  - 每种作物只选 1-2 个高频病虫害，并解释选择依据是中国公开数据可得性、物候差异清晰、竞赛叙事完整。
- 3. 数据集准备方案：
  - 详细列出 5 类数据：标签数据、历史气象、未来 7 天天气预报、物候数据、地区静态特征。
  - 明确样本粒度优先级：地区-日 > 地区-周。
  - 给出推荐字段清单：`date`、`crop`、`disease`、`region_id`、`agro_zone`、`lat/lon`、`phenology_stage`、`temp_mean_3d`、`humidity_mean_3d`、`rain_sum_7d`、`gdd`、`label`。
  - 单独写明 YOLO 图片识别数据只作辅助观测证据，不作为趋势预测主标签。
- 4. 地区特征与区域划分：
  - 主划分采用农业生态区，不只按地貌区。
  - 默认全国一级分区为：东北区、黄淮海区、长江中下游区、华南区、西南区、西北灌溉区。
  - 同时保留省份、纬度、经度、海拔、常年气候均值作为辅助特征，避免纯硬分区带来的信息损失。
  - 按作物补充专属分区示例：
    - 小麦：黄淮海冬麦区、长江中下游冬麦区、西北春麦区、西南麦区
    - 水稻：东北单季稻区、长江中下游稻区、华南双季稻区、西南稻区
    - 玉米：东北春玉米区、黄淮海夏玉米区、西南玉米区、西北灌溉区
  - 明确写出分区颗粒度选择规则：样本少时用 4-6 大区，样本增多后再细化。
- 5. 模型方案：
  - 主方案固定为“农业规则先验 + LightGBM/XGBoost”。
  - 先验层写入物候敏感期和气象阈值，如高湿、连续降雨、适温区间命中。
  - 模型层使用滑窗天气特征、阈值特征、物候特征、地区特征预测风险分数或爆发概率。
  - 7 天趋势生成逻辑写清楚：对未来 1-7 天逐日构造天气特征并独立预测，拼成趋势线，不走纯递推时序。
  - LSTM 作为对比实验单列一节，说明其适用前提是大规模连续时序样本，当前不作为主模型。
- 6. 数据量与评估标准：
  - 写入最低可用数据量：单个作物病种 3000-8000 条样本，正样本至少 300-500 条。
  - 写入较稳妥数据量：8000-20000 条，覆盖 2-3 年、6 个以上区域。
  - 评估采用时间切分，禁止随机打散。
  - 指标固定为 `F1`、`PR-AUC`、`Recall@HighRisk`、趋势相关性、校准表现。
- 7. 系统落地与接口：
  - 后端新增一个趋势预测接口，文档内写清输入输出。
  - 接口固定为：
    - `POST /api/trend/forecast`
    - 输入：`crop`、`region_code`、`target_date`、`forecast_days`、`sowing_date` 或 `phenology_stage`
    - 输出：`today_risk_score`、`today_risk_level`、`daily_series`、`top_drivers`、`model_version`
  - 前端展示固定为趋势图、风险卡片、主导因子解释、7 天建议，不再保留多套“模型切换” mock 叙事。
- 8. 里程碑与风险：
  - 里程碑分为数据整理、特征工程、基线模型、前后端接入、答辩包装五阶段。
  - 风险部分写清：标签不统一、公开病虫害数据离散、物候缺失、地区分区过细导致样本不足。
  - 每项风险配一个默认缓解策略。

## 关键接口与可视化约束
- 文档中将新增唯一核心接口定义 `POST /api/trend/forecast`，作为 Web 与移动端共用接口。
- 图表输出统一为未来 7 天风险趋势折线，背景可叠加降雨柱状图；解释层统一使用 `top_drivers` 展示“湿度偏高、连续降雨、处于敏感物候期”等原因。
- 预测目标统一为风险分数/风险等级，不写成精确病害发生量预测，避免数据与指标不匹配。

## Test Plan
- 文档内应列出至少 5 个验证场景：
  - 南方高湿高温且处于敏感物候期时，水稻风险上升。
  - 北方小麦在拔节或抽穗期，同样天气下风险高于非敏感期。
  - 同样天气输入南北不同农业区，输出风险不能相同。
  - 缺少本地历史观测时，仅用天气和物候仍能输出趋势。
  - 未来天气转干燥降温时，7 天风险曲线应回落。
- 额外要求文档写清训练验证集按时间切分，防止数据泄漏。

## Assumptions
- 默认文档使用中文撰写，风格偏正式技术方案。
- 默认文件名为 `病虫害趋势预测竞赛版方案.md`，与原 `病虫害预测系统落地计划.md` 并存。
- 默认首版作物为水稻、玉米、小麦；若后续要贴合现有前端作物，再单独补一版映射说明。
- 默认不在文档中展开真实数据源链接清单，只先定义数据类型、字段、样本量、区域划分和模型路线；下一步再补“可采集数据源目录”。
