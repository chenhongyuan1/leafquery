这份《多维气象驱动的病虫害时空预测与全景大屏系统》总体落地计划，整合了我们之前讨论的所有核心优化（防数据污染、多病害并发推演、全景可视化、大模型统筹）。

这是一个达到商用级别（SaaS）的 MLOps 工程方案。

# ---

**核心架构蓝图：双轨制协同引擎**

本系统彻底摒弃了“单次算命式”的粗暴预测，采用\*\*“双轨制（服务轨 \+ 训练轨）”\*\*架构，确保系统既能提供实时的前沿智能体验，又能安全、稳定地实现自我进化。

* **视觉感知层**：YOLOv8 承担高精度图像识别（确诊当前状况）。  
* **气象基座层**：和风天气 API 提供过去 10 天至未来 7 天的时空环境数据 。

* **趋势推演层**：按作物拆分的 **XGBoost 独立模型集群（One-vs-Rest）**，基于气象滑动窗口特征并发推演未来趋势 。

* **专家决策层**：LLM 大模型接收多种并发病害的概率，输出兼顾防虫与防病的“统筹处方”。

# ---

**总体实施计划 (Implementation Plan)**

## **Phase 1：基座搭建与冷启动防御（服务轨跑通）**

**目标**：不依赖任何历史机器学习数据，先让系统上线并具备强大的“专家级”预警能力。

1. **和风天气 API 接入与缓存机制**：  
   * 对接 GET /v7/weather/7d（未来预报）和 GET /v7/historical/weather（历史时光机）。

   * 实现 Ed25519 签名的 JWT 认证 。

   * 开发\*\*空间缓存（GeoHash）\*\*机制，避免同区域气象接口的重复调用，解决并发请求性能瓶颈。  
2. **农学规则冷启动引擎 (Rule-based Engine)**：  
   * 在初期无数据时，将农学文献转化为 Java if-else 配置。例如：前 3 天均温 temp\_avg\_3d 在适宜区间且前 5 天最大湿度 humidity\_max\_5d 超标，触发预警 。

3. **LLM 气象推理中枢**：  
   * 将规则引擎得出的高危结果与当地天气，注入 LLM Prompt。  
   * 建立 Prompt 安全沙盒，过滤用户的注入攻击词汇。

## **Phase 2：钛合金数据漏斗（训练轨防污染）**

**目标**：建立极其严苛的数据清洗防线，为 XGBoost 收集极其纯净的本地化训练样本。

1. **文件安全拦截**：  
   * Java 后端引入 Apache Tika，校验文件 Magic Number，防止恶意脚本伪装成 .jpg 上传。  
2. **EXIF 物理溯源与脱敏**：  
   * 使用 metadata-extractor 提取照片的 DateTimeOriginal。非当日现拍照片，直接标记为 is\_dirty \= true，阻断入库。  
   * 提取 GPS 经纬度，并立刻降维转化为 **GeoHash**（抹除精准位置保护隐私，但保留区域气象特征）。  
3. **AI 交叉熔断**：  
   * YOLOv8 识别置信度低于 95% 的数据抛弃。  
   * 反气象常识的记录抛弃。  
   * 通过漏斗的纯净数据，打上多标签（如 {晚疫病:1, 早疫病:0}）存入 training\_data。

## **Phase 3：并发预测引擎建设（XGBoost 登场）**

**目标**：用积累的纯净数据，替换初期的静态规则，实现“一株多病”的精准推演。

1. **特征工程流线化**：  
   * 构建提取 crop\_type 、气象滑动窗口（如 rain\_sum\_7d） 组合特征的自动化 Pipeline。

2. **多模型并发架构 (One-vs-Rest)**：  
   * 针对一种作物（如番茄），并行训练 N 个轻量级 XGBoost 二分类器。  
   * Java 端设计工厂模式，实现模型文件的“热插拔”与并发调度。  
3. **动态加载与影子部署**：  
   * 设计模型版本控制表。新训练的模型先进入“影子模式”后台试运行，人工确认指标正常后，一键无缝热替换线上主模型。

## **Phase 4：全景大屏与统筹决策（前端可视化）**

**目标**：将冰冷的概率数据转化为极具视觉冲击力与业务价值的农情大屏。

1. **并发风险时序图 (Vue \+ ECharts)**：  
   * 实现双 Y 轴折线图。背景柱状图展示未来 7 天降水，前景多条平滑曲线展示不同病虫害的爆发概率走势。  
2. **归因雷达图 (ECharts)**：  
   * 结合 XGBoost 输出的主导特征，动态渲染“致病三角”雷达图，直观揭示诱发病害的“元凶”（如持续高湿）。  
3. **空间热力图 (Leaflet.js)**：  
   * 在地图上叠加以农田为中心的气象风险渐变图。  
4. **LLM 复合处方生成**：  
   * 将并发的高危病害组合（如：晚疫病 80% \+ 蚜虫 65%）送入大模型，输出“杀虫防病双效兼容”的综合药单，并由前端 marked 渲染为带排版的结构化建议。

## ---

**下一步行动建议**

这个计划涵盖了从后端数据清洗到前端炫酷展示的完整链路。为了脚踏实地开始编码，我们必须先打好地基。

你想先从哪一部分的代码实现开始？

1. 设计**底层 MySQL 数据库表结构**（包含支持 GeoHash 和模型版本控制的字段）？  
2. 编写 Java 后端**解析 EXIF 并构建防污染漏斗**的代码？  
3. 编写前端 Vue 结合 ECharts 的**并发风险趋势折线图** Demo？